<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.cmn.projectminhasfinancas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="info.cmn.projectminhasfinancas.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="info.cmn.projectminhasfinancas.ListarActivity" />
        <activity android:name="info.cmn.projectminhasfinancas.LoginActivity" />
        <activity android:name="info.cmn.projectminhasfinancas.MenuActivity" />
        <activity android:name="info.cmn.projectminhasfinancas.ReceitasActivity"></activity>

        <activity android:name="info.cmn.projectminhasfinancas.myCalendarView" />
        <activity android:name="info.cmn.projectminhasfinancas.MainCalendarActivity" />

    </application>

</manifest>